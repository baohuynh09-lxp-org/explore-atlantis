version: 3
projects:
- name: dev-sandbox_network
  dir: deployment/network
  workflow: network
  apply_requirements: [mergeable] #Requirements that must be satisfied before atlantis apply can be run
  autoplan:
    enabled: false

- name: dev-sandbox_jumphost
  dir: deployment/jumphost
  workflow: jumphost
  apply_requirements: [mergeable] #Requirements that must be satisfied before atlantis apply can be run
  autoplan:
    enabled: false

workflows:
  network:
    plan:
      steps:
      - run: |
          rm -rf .terraform
          ls -la
      #    #TF_ARG=$(echo $COMMENT_ARGS | tr ',' ' ' | sed -r 's/\\(.)/\1/g')
      #    #echo "TF_ARG: $TF_ARG"
      #    TEST1=$(printf '%b\n' "$COMMENT_ARGS")
      #    echo "COMMENT_ARGS: $COMMENT_ARGS"
      #    echo "TEST1: $TEST1"

      - init:
          extra_args: [-backend-config=backends/dev-sandbox.backend.tf]
      - plan:
          extra_args: [-var-file=../env/dev-sandbox.tfvars]

      #- run: |
      #    eval "terraform init -backend-config=../backends/${TF_ARG}.backend.tf"
      #- run: |
      #    eval "terraform plan -var-file=../env/${COMMENT_ARGS}.tfvars"

    #apply:
    #  steps:
    #  #- run: |
    #  #    rm -rf .terraform 
    #  - init:
    #      extra_args: [-backend-config=backends/dev-sandbox.backend.tf]
    #  - apply:
    #      extra_args: [-var-file=../env/dev-sandbox.tfvars]


  jumphost:
    plan:
      steps:
      - run: |
          rm -rf .terraform
          echo "COMMENT_ARGS: $COMMENT_ARGS"
      - init:
          extra_args: [-backend-config=../backends/dev-sandbox.backend.tf]
      - plan:
          extra_args: [-var-file=../env/dev-sandbox.tfvars]

    apply:
      steps:
      - run: |
          rm -rf .terraform
          echo "COMMENT_ARGS:# $COMMENT_ARGS"
      - init:
          extra_args: [-backend-config=../backends/dev-sandbox.backend.tf]
      - plan:
          extra_args: [-var-file=../env/dev-sandbox.tfvars]
